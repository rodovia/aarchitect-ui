func main()
{
    var fib = Fiber.create({
        var i = 0;
        System.print("1");
        Fiber.yield();
        while (i < 100)
        {
            Fiber.yieldWaitTime(2.0);
            System.print("ww");
            i += 1;
        }
        System.print("2");
        Fiber.yield();
    });

    fib.call();
    fib.call();
    

}