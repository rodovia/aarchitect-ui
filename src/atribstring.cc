#include "atribstring.h"
#include <cstring>

void aarSetAttribArray(uiAttributedString *string, const char *key, uiAttribute *value) {
#if 0
    size_t start, end;

    start = uiAttributedStringLen(string);
    end = start + std::strlen(key);
    uiAttributedStringAppendUnattributed();
#endif
}
